<!doctype html>
<html lang="de">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Landing Page — Klick mich</title>
  <style>
    /* Reset light */
    * { box-sizing: border-box; margin: 0; padding: 0; }

    html,body { height: 100%; font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial; color: #222; background: linear-gradient(180deg,#f7fbff 0%, #eef6ff 100%); }

    .container {
      min-height: 100%;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 48px 20px;
    }

    .card {
      width: 100%;
      max-width: 920px;
      background: white;
      border-radius: 16px;
      box-shadow: 0 8px 30px rgba(20,40,80,0.08);
      padding: 40px;
      display: grid;
      grid-template-columns: 1fr 360px;
      gap: 32px;
      align-items: center;
    }

    @media (max-width: 820px) {
      .card { grid-template-columns: 1fr; padding: 28px; }
    }

    .headline {
      margin-bottom: 16px;
    }
    .headline h1 {
      font-size: 28px;
      line-height: 1.05;
      margin-bottom: 8px;
    }
    .headline p {
      color: #555;
      font-size: 15px;
    }

    .feature-list {
      margin-top: 18px;
      display: grid;
      gap: 10px;
    }
    .feature-item { display:flex; gap:10px; align-items:center; color:#444; }
    .dot {
      width:12px; height:12px; border-radius:50%;
      background: linear-gradient(135deg,#6ad7ff,#6ac1ff);
      box-shadow: 0 2px 6px rgba(90,160,220,0.12);
    }

    /* Button area */
    .action {
      display:flex;
      flex-direction:column;
      gap:16px;
      align-items:center;
    }

    .cta {
      -webkit-tap-highlight-color: transparent;
      display:inline-flex;
      align-items:center;
      justify-content:center;
      gap:10px;
      padding: 14px 22px;
      font-weight:600;
      font-size:16px;
      border-radius:10px;
      border: none;
      cursor:pointer;
      background: linear-gradient(90deg,#2a9df4,#2b8ae3);
      color: white;
      box-shadow: 0 8px 20px rgba(43,138,227,0.18), inset 0 -2px 0 rgba(0,0,0,0.06);
      transition: transform .12s ease, box-shadow .12s ease;
    }
    .cta:active { transform: translateY(1px) scale(.998); }
    .cta:focus { outline: 3px solid rgba(43,138,227,0.18); outline-offset: 3px; }

    .secondary {
      background: transparent;
      color: #2a6aa8;
      font-weight:600;
      border: 2px solid rgba(42,109,160,0.12);
      padding: 10px 16px;
      border-radius: 10px;
    }

    .klick-info {
      margin-top: 6px;
      color:#345;
      font-size:14px;
    }

    /* Modal */
    .modal-backdrop {
      position: fixed;
      inset: 0;
      display: none;
      align-items: center;
      justify-content: center;
      background: rgba(8,16,32,0.45);
      z-index: 1200;
      padding: 20px;
    }
    .modal {
      background: white;
      border-radius: 12px;
      max-width: 520px;
      width: 100%;
      padding: 22px;
      box-shadow: 0 20px 50px rgba(15,30,60,0.25);
    }
    .modal h2 { margin-bottom: 8px; font-size:20px; }
    .modal p { color:#444; margin-bottom: 16px; }

    .close {
      background: transparent;
      border: none;
      font-weight:700;
      font-size:16px;
      color:#666;
      cursor:pointer;
    }

    footer.small {
      margin-top: 20px;
      font-size:12px;
      color:#7a8693;
      text-align:center;
    }
  </style>
</head>
<body>
  <main class="container" role="main">
    <section class="card" aria-labelledby="headline">
      <div>
        <div class="headline" id="headline">
          <h1>Willkommen auf unserer Landing-Page</h1>
          <p>Ein schneller Demo-Prototyp mit einem interaktiven Button — klick ihn an und sieh, was passiert.</p>
        </div>

        <div class="feature-list" aria-hidden="false">
          <div class="feature-item"><span class="dot" aria-hidden="true"></span><span>Schnell & responsiv</span></div>
          <div class="feature-item"><span class="dot" aria-hidden="true"></span><span>Barrierefrei & keyboard-freundlich</span></div>
          <div class="feature-item"><span class="dot" aria-hidden="true"></span><span>Leicht anpassbar</span></div>
        </div>

        <footer class="small" aria-hidden="true">Tweak the code to fit your brand — Farben, Texte, Aktionen.</footer>
      </div>

      <div class="action" aria-label="Aktionen">
        <button id="primaryBtn" class="cta" aria-describedby="klickInfo">
          🚀 Jetzt klicken
        </button>

        <div id="klickInfo" class="klick-info" aria-live="polite">
          Du hast den Button noch nicht geklickt.
        </div>

        <button id="secondaryBtn" class="secondary" aria-haspopup="dialog">
          Mehr Infos
        </button>
      </div>
    </section>
  </main>

  <!-- Modal -->
  <div id="modalBackdrop" class="modal-backdrop" role="dialog" aria-modal="true" aria-hidden="true" aria-labelledby="modalTitle">
    <div class="modal">
      <div style="display:flex; justify-content:space-between; align-items:center;">
        <h2 id="modalTitle">Mehr Informationen</h2>
        <button class="close" id="modalClose" aria-label="Schließen">✕</button>
      </div>
      <p>Danke fürs Anschauen — dieses kleine Modal zeigt, wie man zusätzliche Inhalte einblendet. Du kannst hier Links, Formulare oder andere Interaktionen platzieren.</p>
      <p style="font-size:13px;color:#667">Beispiel: Weiterleitung nach Klick, Newsletter-Signup oder Download.</p>
      <div style="display:flex; gap:8px; justify-content:flex-end; margin-top:8px;">
        <button id="modalAction" class="cta" style="padding:10px 14px;">Aktion ausführen</button>
        <button class="secondary" id="modalCancel">Abbrechen</button>
      </div>
    </div>
  </div>

  <script>
    // Einfaches, robustes Verhalten für Buttons
    (function() {
      const primaryBtn = document.getElementById('primaryBtn');
      const secondaryBtn = document.getElementById('secondaryBtn');
      const klickInfo = document.getElementById('klickInfo');

      const modalBackdrop = document.getElementById('modalBackdrop');
      const modalClose = document.getElementById('modalClose');
      const modalCancel = document.getElementById('modalCancel');
      const modalAction = document.getElementById('modalAction');

      let clicks = 0;

      // Hauptbutton: zählt Klicks und zeigt kurzes Feedback
      primaryBtn.addEventListener('click', () => {
        clicks += 1;
        if (clicks === 1) {
          klickInfo.textContent = 'Danke! Du hast 1x geklickt.';
        } else {
          klickInfo.textContent = `Danke! Du hast ${clicks}x geklickt.`;
        }

        // kleine visuelle "pulse" Animation
        primaryBtn.animate([
          { transform: 'scale(1)' },
          { transform: 'scale(1.04)' },
          { transform: 'scale(1)' }
        ], {
          duration: 280,
          easing: 'cubic-bezier(.2,.9,.3,1)'
        });

        // Beispiel: nach 5 Klicks etwas anderes tun (nur Demo)
        if (clicks === 5) {
          alert('🎉 5 Klicks erreicht — gut gemacht!');
        }
      });

      // Modal öffnen
      function openModal() {
        modalBackdrop.style.display = 'flex';
        modalBackdrop.setAttribute('aria-hidden', 'false');
        // Fokusmanagement: fokus auf erstem Fokus-Element
        modalClose.focus();
        // Trap focus (sehr einfacher trap)
        document.addEventListener('focus', trapFocus, true);
      }

      function closeModal() {
        modalBackdrop.style.display = 'none';
        modalBackdrop.setAttribute('aria-hidden', 'true');
        document.removeEventListener('focus', trapFocus, true);
        secondaryBtn.focus();
      }

      secondaryBtn.addEventListener('click', openModal);
      modalClose.addEventListener('click', closeModal);
      modalCancel.addEventListener('click', closeModal);

      modalAction.addEventListener('click', () => {
        // Beispielaktion: Weiterleitung (kommentiert). Du kannst hier AJAX / Form etc.
        // window.location.href = 'https://example.com';
        alert('Modal-Aktion ausgeführt (Demo).');
        closeModal();
      });

      // Schließen mit Escape
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && modalBackdrop.style.display === 'flex') {
          closeModal();
        }
      });

      // Simple focus trap: falls Fokus außerhalb Modal, bring ihn zurück
      function trapFocus(e) {
        if (modalBackdrop.style.display !== 'flex') return;
        if (!modalBackdrop.contains(e.target)) {
          e.stopPropagation();
          modalClose.focus();
        }
      }

      // Accessibility: Enter/Space on primaryBtn works by default for button elements
    })();
  </script>
</body>
</html>